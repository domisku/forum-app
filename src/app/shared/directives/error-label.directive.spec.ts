import { ErrorLabelDirective } from './error-label.directive';

describe('ErrorLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorLabelDirective();
    expect(directive).toBeTruthy();
  });
});
